  <p class='comment-body'><%= comment.body %></p>
  <p class='commenter'>-<%= comment.name %></p>
  <p class='time'><%= time_ago_in_words(comment.created_at) %> ago
  <% if logged_in? && admin? %>
    | <%= link_to 'Delete', [comment.post, comment], method: :delete %></p>
  <% end %>

