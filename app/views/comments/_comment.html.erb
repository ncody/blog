<div class='comments'>
  <p><%= comment.body %></p>
  <p><%= comment.name %></p>
  <p><%= time_ago_in_words(comment.created_at) %> ago
  <% if admin? %>
    | <%= link_to 'Delete', [comment.post, comment], method: :delete %></p>
  <% end %>
</div>
